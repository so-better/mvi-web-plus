import { withInstall } from '@/utils'
import collapse from './collapse.vue'

const Collapse = withInstall(collapse)

export type * from '@/components/collapse/props'
export { Collapse, Collapse as default }
